# Скопируйте этот файл в <SECOND_BRAIN_ROOT>/runtime/.env с mode 600.
# Выбирайте его явно через --env-file; относительный путь считается от каталога env-файла.
SECOND_BRAIN_VAULT_PATH=../second-brain-vault
